---
const { title, text, icon } = Astro.props;
---

<li>
  <i class={`${icon} text-4xl text-teal-800 mb-1`}></i>
  <h3 class="text-xl font-semibold mb-1">{title}</h3>
  <p class="text-lg">
    {text}
  </p>
</li>
